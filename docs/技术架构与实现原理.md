# Simple Homepage 技术架构与实现原理

> 本文档面向开发者和高级用户，深入讲解项目架构、数据流、安全模型和关键实现细节。
> 
> 版本：基于当前代码现状整理 | 更新日期：2026-04

---

## 目录

1. [整体架构](#整体架构)
2. [模块划分总览](#模块划分总览)
3. [前台模块详解](#前台模块详解)
4. [后台模块详解](#后台模块详解)
5. [CLI 模块](#cli-模块)
6. [请求生命周期](#请求生命周期)
7. [认证与安全模型](#认证与安全模型)
8. [数据存储设计](#数据存储设计)
9. [Nginx 反向代理自动生成](#nginx-反向代理自动生成)
11. [子站 SSO 机制](#子站-sso-机制)
12. [通知与 Webhook 架构](#通知与-webhook-架构)
13. [DDNS 执行流程](#ddns-执行流程)
14. [计划任务调度原理](#计划任务调度原理)
15. [关键数据流](#关键数据流)
16. [测试架构分层](#测试架构分层)
17. [接口契约](#接口契约)
18. [部署环境变量](#部署环境变量)
19. [安全注意事项](#安全注意事项)

---

## 整体架构

```text
┌─────────────────────────────────────────────────────────────┐
│                         用户浏览器                           │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│  前置 Nginx / Caddy / Traefik（可选，域名+HTTPS场景）        │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│  Docker 容器（单容器方案）                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Nginx（navwww 用户运行）                              │    │
│  │  ├── 静态文件直接返回                                  │    │
│  │  ├── .php → PHP-FPM（Unix Socket）                    │    │
│  │  ├── /admin/ → auth_request 鉴权 + PHP-FPM            │    │
│  │  ├── /p/{slug}/ → proxy_pass 内网服务                 │    │
│  │  └── /auth/verify.php → internal 鉴权接口             │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  PHP-FPM（navwww 用户运行）                            │    │
│  │  ├── public/   前台入口                                │    │
│  │  ├── admin/    后台管理                                │    │
│  │  ├── shared/   核心认证/通知/计时库                    │    │
│  │  ├── cli/      CLI 脚本与守护进程                      │    │
│  │  └── subsite-middleware/ 子站鉴权中间件                │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Supervisor（PID 1）                                   │    │
│  │  └── 同时管理 Nginx + PHP-FPM + Cron                  │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────┬───────────────────────────────────┘
                          │  ./data:/var/www/nav/data
┌─────────────────────────▼───────────────────────────────────┐
│  宿主机数据目录（持久化）                                     │
│  ├── config.json / sites.json / users.json                  │
│  ├── scheduled_tasks.json / dns_config.json                 │
│  ├── backups/ / logs/ / tasks/ / favicon_cache/             │
│  └── ...                                                    │
└─────────────────────────────────────────────────────────────┘
```

### 架构选型说明

项目采用**单容器 Alpine 方案**（`php:8.2-fpm-alpine` + Nginx + Supervisor），核心原因：

- 导航站属于轻量级应用，无需微服务分离
- Alpine 镜像体积极小（基础 ~80MB，最终 ~150MB），适合 NAS、软路由、VPS 等资源受限场景
- Supervisor 统一管理进程，崩溃自动重启
- 一个 `docker compose up` 即可拉起，运维简单
- 支持 `linux/amd64` 和 `linux/arm64` 双架构

---

## 模块划分总览

```text
simple-homepage/
├── public/                 # 前台入口
├── admin/                  # 后台管理
│   ├── *.php               # 业务页面
│   ├── api/                # 后台 API
│   ├── shared/             # 后台公共库
│   └── assets/             # 后台静态资源
├── shared/                 # 全站共享核心库
├── cli/                    # 命令行工具与守护进程
├── subsite-middleware/     # 子站鉴权中间件
├── python/                 # DNS Python 核心（可选）
├── docker/                 # 容器构建与运行配置
├── nginx-conf/             # Nginx 配置模板
├── tests/                  # Playwright E2E + PHPUnit
└── data/                   # 运行时数据目录（挂载卷）
```

### 模块职责矩阵

| 模块 | 路径 | 职责 |
|------|------|------|
| 前台模块 | `public/` | 安装向导、登录、首页、资源代理、公共 API、 |
| 后台模块 | `admin/` | 站点/分组/用户/设置/备份/DNS/DDNS/计划任务/调试/宿主运维/文件管理/Docker//通知 |
| 共享核心 | `shared/` | 认证、通知运行时、请求耗时记录 |
| CLI 层 | `cli/` | DNS 同步、DDNS 同步、计划任务执行、健康检查、过期扫描 |
| 中间件 | `subsite-middleware/` | 子站统一鉴权中间件 |
| 测试层 | `tests/e2e/full/` | Playwright Full E2E 回归测试（159 个 spec） |
| 单元测试 | `tests/phpunit/` | PHPUnit 单元测试 |

---

## 前台模块详解

### 入口文件清单

| 文件 | 职责 | 关键依赖 |
|------|------|----------|
| `setup.php` | 首次安装向导，完成后 404 封闭 | `shared/auth.php` |
| `index.php` | 导航首页：分组展示、搜索、过滤、健康状态、代理生效提示 | `shared/auth.php`, `sites.json` |
| `login.php` | 登录页：表单提交、CSRF、IP 锁定、Token 生成、记住我 | `shared/auth.php` |
| `logout.php` | 退出登录：CSRF 校验、清除 Cookie、写审计日志 | `shared/auth.php` |
| `auth/verify.php` | Nginx `auth_request` 验证接口（internal） | `shared/auth.php` |
| `api/dns.php` | 公共 DNS HTTP API：query / update / batch_update | `admin/shared/dns_api_lib.php` |
| `bg.php` | 背景图安全代理（白名单文件名 + MIME 校验） | — |
| `favicon.php` | Favicon 抓取代理（域名白名单 + 魔数校验 + 缓存） | — |
| `.php` |  服务端（Basic Auth + 配额 + 审计） | `shared/auth.php` |
| `.php` | 通知探针接收端（用于测试 Webhook） | — |

### 首页渲染逻辑

```text
index.php
  ├─ auth_check_setup()          # 是否需要安装向导
  ├─ load_sites() / sites.json   # 读取站点分组
  ├─ auth_get_current_user()     # 当前用户信息
  ├─ health_load_cache()         # 健康状态缓存（仅登录用户）
  ├─ 过滤可见分组（auth_required / visible_to）
  ├─ 渲染卡片（external / internal / proxy）
  └─ 输出内联 CSS/JS（响应式 + 搜索过滤 + 最近访问）
```

### 站点跳转规则

| 站点类型 | 跳转逻辑 |
|----------|----------|
| `external` | 直接跳转 `url` |
| `internal` | 跳转 `url?_nav_token={TOKEN}` |
| `proxy` (path) | 跳转 `/p/{slug}/` |
| `proxy` (domain) | 跳转 `https://{proxy_domain}/` |

---

## 后台模块详解

### 页面层（直接面向浏览器的 PHP 文件）

#### 内容管理

| 文件 | 职责 |
|------|------|
| `sites.php` | 站点 CRUD、类型切换、代理字段联动 |
| `groups.php` | 分组 CRUD、排序、可见性控制 |
| `users.php` | 用户 CRUD、角色切换、密码修改 |

#### 系统与设置

| 文件 | 职责 |
|------|------|
| `settings.php` | 全局配置保存、导入导出、备份恢复、Nginx 生成、Webhook 测试 |
| `settings_ajax.php` | 惰性 AJAX 接口（nginx_sudo、health_sites_meta）|
| `backups.php` | 备份列表、创建、恢复、删除、下载 |
| `debug.php` | 调试开关、日志查看、清 Cookie、构建信息比对 |

#### 运维相关

| 文件 | 职责 |
|------|------|
| `dns.php` | DNS 账户 / Zone / 记录管理、批量导入导出 |
| `ddns.php` | DDNS 任务管理页面 |
| `ddns_ajax.php` | DDNS 异步接口（save/run/test_source/log/log_clear）|
| `scheduled_tasks.php` | 计划任务 CRUD、日志查看、调度器保护 |
| `nginx.php` | Nginx 配置编辑器、语法检测、Reload |

#### 宿主运维（Host-Agent 体系）

| 文件 | 职责 |
|------|------|
| `hosts.php` | 主机列表、本机 SSH 管理 |
| `host_api.php` | 主机相关 AJAX 接口（SSH、文件系统、终端、远程主机）|
| `host_runtime.php` | 宿主机运维面板（系统概览、进程、服务、网络、用户、用户组）|
| `files.php` | 文件管理器页面（本机 + 远程主机）|
| `file_api.php` | 文件操作 AJAX 接口 |
| `docker_hosts.php` | Docker 宿主管理页面 |
| `docker_api.php` | Docker 相关 AJAX 接口 |

#### 审计与辅助

| 文件 | 职责 |
|------|------|
| `login_logs.php` | 登录日志展示 |
| `health_check.php` | 站点健康检测页面与 API |
| `` | 域名/SSL 到期管理 |
| `` | 通知渠道配置 |
| `share_service_audit.php` | 共享服务审计 |
| `ssh_audit.php` | SSH 操作审计 |
| `file_audit.php` | 文件操作审计 |
| `.php` / `_audit.php` / `_shares.php` |  管理、审计、共享 |

### 后台共享库（admin/shared/）

这是后台的业务逻辑核心层。

| 文件 | 职责 | 核心函数/类 |
|------|------|-------------|
| `functions.php` | 站点数据读写、系统配置、CSRF、Flash、备份恢复、健康检测、Webhook、Nginx 配置生成 | `load_sites()`, `save_sites()`, `csrf_token()`, `backup_create()`, `health_check_all()`, `nginx_generate_proxy_conf()` |
| `dns_lib.php` | DNS 配置读写、日志 | `load_dns_config()`, `dns_log_write()` |
| `dns_api_lib.php` | DNS HTTP API 核心：Zone 缓存、解析记录 upsert/query/batch | `dns_api_upsert()`, `dns_api_query()`, `dns_api_batch_update()` |
| `ddns_lib.php` | DDNS 任务核心：IP 来源选择、DNS 更新、任务执行 | `ddns_run_task()`, `ddns_due_tasks()` |
| `cron_lib.php` | 计划任务核心：crontab 同步、脚本落地、任务执行 | `cron_regenerate()`, `cron_run_task_by_id()`, `task_resolve_command_text()` |
| `.php` | 域名/SSL 到期扫描 | `expiry_scan_and_store()` |
| `notify_lib.php` | 通知渠道 Webhook 发送（旧版，与 `shared/.php` 并存） | `notify_send()` |
| `share_service_lib.php` | 共享服务（SFTP/SMB/FTP/NFS/AFP/Async）配置管理 | — |
| `alidns.php` | 阿里云 DNS SDK 封装 | `AliyunDnsClient` |
| `_lib.php` |  配置与共享管理 | — |
| `header.php` / `footer.php` | 后台统一页面布局 | — |

### 共享核心模块（shared/）

该目录文件被 `public/`、`admin/`、`cli/`、`subsite-middleware/` 共同引用，是项目的"底层操作系统"。

#### auth.php（认证核心库）

**文件规模**：~1300 行  
**核心职责**：Token 生成验证、Cookie 管理、用户验证、IP 锁定、登录日志、首次安装检测、配置读取。

| 分组 | 代表函数 | 说明 |
|------|----------|------|
| 密钥管理 | `auth_secret_key()`, `auth_rotate_secret_key()` | 64B hex 随机密钥，持久化到 `data/auth_secret.key` |
| 配置读取 | `auth_get_config()`, `auth_default_config()` | 从 `config.json` 读取，缺失字段自动补默认值 |
| 安装检测 | `auth_needs_setup()`, `auth_apply_initial_install()` | `.installed` + `users.json` 双重判断 |
| Token | `auth_generate_token()`, `auth_verify_token()` | JWT-like HMAC-SHA256，含 jti 防重放 |
| 用户管理 | `auth_load_users()`, `auth_save_user()`, `auth_check_password()` | JSON 文件存储，bcrypt 哈希（cost=10） |
| 权限系统 | `auth_user_permissions()`, `auth_user_has_permission()` | 角色权限 + 自定义权限混合模型 |
| 开发模式 | `auth_dev_mode_enabled()`, `auth_dev_qa_user_record()` | 注入内存虚拟用户 `qatest`，不落盘 |
| IP 锁定 | `ip_locks_load()`, `ip_locks_save()` | 自动清理过期记录 |
| 客户端 IP | `get_client_ip()` | 支持 X-Real-IP、X-Forwarded-For 链、REMOTE_ADDR |
| Cookie 策略 | `auth_cookie_domain_for_request()`, `auth_cookie_secure_for_request()` | IP 访问自动降级，HTTPS 自适应 |
| 重定向安全 | `auth_sanitize_redirect()` | 防开放重定向，仅允许本站群地址 |

---

## CLI 模块

### 脚本清单

| 脚本 | 职责 | 调度方式 |
|------|------|----------|
| `alidns_sync.php` | 阿里云 DNS 记录同步，更新 `last_sync_at` | cron / 手动 |
| `ddns_sync.php` | DDNS 任务执行：单任务或批量 due tasks | cron / 手动 |
| `run_scheduled_task.php` | 计划任务统一执行入口 | cron / 后台立即执行 |
| `.php` | 域名/SSL 到期扫描（可选 `--notify`） | cron |

### CLI 设计模式

- **非 CLI 访问统一 403**：所有脚本开头检查 `php_sapi_name() !== 'cli'`。
- **退出码契约**：
  - `0` = 成功
  - `1` = 业务失败（配置缺失、任务不存在、API 失败）
  - `2` = 参数/解码错误
- **副作用可观测**：成功/失败均写 stdout/stderr，关键状态持久化到 JSON 文件。

---

## 请求生命周期

### 前台请求

```text
浏览器请求 /index.php
    ↓
Nginx（监听 NAV_PORT，默认 58080）
    ↓
fastcgi_pass unix:/run/php-fpm.sock
    ↓
PHP-FPM worker 加载 public/index.php
    ├── shared/auth.php → auth_check_setup() / auth_get_current_user()
    ├── 读取 sites.json → 过滤可见分组
    ├── 读取 config.json → 获取站点名称、背景、卡片样式
    ├── 可选：读取 health_cache.json → 站点健康状态
    └── 渲染 HTML + 内联 CSS/JS
```

### 后台请求

```text
浏览器请求 /admin/settings.php
    ↓
Nginx location ^~ /admin/ { auth_request /auth/verify.php; }
    ↓
auth_request 子请求 → public/auth/verify.php
    ├── 验证 Cookie 中的 Token
    └── 返回 200 或 401
    ↓
主请求继续 → admin/settings.php
    ├── auth_request off（PHP location 内关闭，避免死锁）
    ├── shared/auth.php → 二次验证 + 获取当前用户
    ├── admin/shared/functions.php → 加载配置/站点/备份逻辑
    └── 渲染后台页面
```

### 关键安全点

- `auth_request` 子请求与主请求可能争抢 PHP-FPM worker 导致 504 死锁
- **修复**：PHP location 内加 `auth_request off;`，由 PHP 自行鉴权

---

## 认证与安全模型

### Token 机制

项目采用 **JWT-like Token**（HMAC-SHA256 签名），不是标准 JWT，但设计理念类似：

```php
$payload = [
    'username' => $username,
    'role'     => $role,
    'iat'      => time(),
    'exp'      => time() + $ttl,
    'jti'      => bin2hex(random_bytes(16)),  // 防重放
];
$token = base64_encode(json_encode($payload)) . '.' . hash_hmac('sha256', ..., AUTH_SECRET_KEY);
```

**特点：**

- `jti`（JWT ID）每次登录唯一，支持会话级撤销
- 撤销列表保存在 `data/sessions.json`
- 有效期默认 8 小时，"记住我"延长至 60 天

### Cookie 策略

```php
setcookie(SESSION_COOKIE_NAME, $token, [
    'expires'  => $expire,
    'path'     => '/',
    'domain'   => $cookie_domain,
    'secure'   => $secure,      // off / auto / on
    'httponly' => true,
    'samesite' => 'Lax',
]);
```

**Secure 三档模式：**

| 模式 | 行为 | 适用场景 |
|------|------|----------|
| `off` | HTTP/HTTPS 均可发送 | 内网调试、IP 访问 |
| `auto` | 自动检测协议（含 X-Forwarded-Proto）| VPS 反代、混合环境 |
| `on` | 仅 HTTPS 发送 | 生产环境全程 HTTPS |

**IP 访问自动降级：** 当 `HTTP_HOST` 为 IP 格式时，代码自动设置 `secure=false, domain=空`，确保内网应急访问始终可用。

### CSRF 防护

```php
function csrf_token(): string {
    if (session_status() === PHP_SESSION_NONE) session_start();
    if (empty($_SESSION['csrf_token'])) {
        $_SESSION['csrf_token'] = bin2hex(random_bytes(32));
    }
    return $_SESSION['csrf_token'];
}

function csrf_check(): void {
    $token = $_POST['_csrf'] ?? '';
    $expected = $_SESSION['csrf_token'] ?? '';
    if (!$token || !$expected || !hash_equals($expected, $token)) {
        http_response_code(403);
        exit('CSRF验证失败');
    }
}
```

- 所有修改数据的操作（包括登录、退出、安装向导）都必须验证 CSRF Token
- AJAX 鉴权失败返回 JSON（401），不能 302 重定向

### IP 锁定

连续登录失败 5 次后，自动锁定该 IP 15 分钟。记录保存在 `data/ip_locks.json`，每次登录时自动清理过期记录。

### 密码安全

```php
$hash = password_hash($password, PASSWORD_BCRYPT, ['cost' => 10]);
password_verify($input, $hash);
```

### 密钥管理

`AUTH_SECRET_KEY` 优先级：

1. 环境变量 `AUTH_SECRET_KEY`
2. `data/auth_secret.key`（64B hex 随机字符串，权限 600）
3. 安装向导自动生成

---

## 数据存储设计

项目**无数据库依赖**，所有配置、用户、站点数据均持久化为 JSON 文件。

### 数据文件总览

| 文件路径 | 结构 | 用途 |
|----------|------|------|
| `data/.installed` | 时间戳文本 | 安装完成锁 |
| `data/users.json` | `{username: {password_hash, role, created_at, updated_at}}` | 用户账户 |
| `data/config.json` | 扁平键值对象 | 系统配置 |
| `data/sites.json` | `{groups: [{id, name, icon, order, auth_required, visible_to, sites: [...]}]}` | 站点与分组 |
| `data/scheduled_tasks.json` | `{tasks: [...]}` | 计划任务定义和运行结果 |
| `data/dns_config.json` | `{version, accounts: [...]}` | DNS 账户配置 |
| `data/ddns_tasks.json` | `{version, tasks: [...]}` | DDNS 任务定义 |
| `data/notifications.json` | `{version, channels: [...]}` | 通知渠道配置 |
| `data/health_cache.json` | `{url: {status, code, ms, checked_at}}` | 站点健康缓存 |
| `data/ip_locks.json` | `{ip: {fails, locked_until, last_fail}}` | IP 登录失败锁定 |
| `data/sessions.json` | `{revoked: [{jti, revoked_at}]}` | 会话撤销记录 |
| `data/auth_secret.key` | 64B hex 字符串 | 认证密钥 |
| `data/file_favorites.json` | `{version, items: [...]}` | 文件管理收藏 |
| `data/file_recent.json` | `{version, items: [...]}` | 文件管理最近访问 |
| `data/_shares.json` | `{shares: [...]}` |  共享配置 |
| `data/logs/*.log` | 纯文本行日志 | 各类日志 |
| `data/backups/backup_*.json` | 统一备份格式 | 备份快照 |
| `data/tasks/` | `.sh` 脚本 + `.log` 日志 | 计划任务工作目录 |
| `data/favicon_cache/` | 二进制图标文件 | Favicon 缓存 |
| `data/bg/` | 图片文件 | 背景图 |
| `data/nginx/` | `.conf` 模板文件 | Nginx 代理参数模板 |

### 读写规范

```php
// 写入
file_put_contents($file,
    json_encode($data, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES),
    LOCK_EX
);

// 读取
$data = json_decode(file_exists($f) ? file_get_contents($f) : '{}', true) ?? [];
```

### 备份统一格式

```json
{
  "created_at": "2026-04-15 10:00:00",
  "trigger": "manual",
  "sites": { ... },
  "config": { ... },
  "scheduled_tasks": { ... },
  "dns_config": { ... },
  "ddns_tasks": { ... },
  "notifications": { ... }
}
```

---

## Nginx 反向代理自动生成

### 设计目标

后台配置的 Proxy 类型站点，需要自动生成 Nginx 反代配置，无需用户手动编辑 Nginx 配置文件。

### 生效流程

```text
admin/sites.php POST save
  → save_sites() 写入 sites.json
  → 页面提示保存成功
  → 首页检测 homepage_pending_proxy_sites() 显示"未生效"横幅
  → admin/settings.php POST nginx_apply_and_reload
    → nginx_generate_proxy_conf() 生成 nav-proxy.conf / nav-proxy-domains.conf
    → admin_run_command('nginx -t && nginx -s reload')
    → 成功则更新 config.json 中的 nginx_last_applied
```

### 两种代理模式

**路径模式**：`https://nav.example.com/p/{slug}/`

```nginx
location ^~ /p/{slug}/ {
    proxy_pass http://{proxy_target}/;
    include /etc/nginx/proxy_params_full.conf;
}
```

**子域模式**：`https://{proxy_domain}/`

```nginx
server {
    listen 58080;
    server_name {proxy_domain};
    location / {
        proxy_pass http://{proxy_target}/;
        include /etc/nginx/proxy_params_full.conf;
    }
}
```

### 安全校验

`proxy_target` 正则白名单限制，仅允许 `http(s)://host:port` 格式，防止 Nginx 配置注入。同时禁止 SSRF：目标地址不能是内网回环地址或私有后缀（如 `.local`）。

---

## 子站 SSO 机制

### 目标

让导航站登录后，子站（同域名下的其他应用）无需再次登录。

### 实现方式

1. **Cookie 共享**：配置 `cookie_domain=.yourdomain.com` 后，导航站与子站共享 Cookie
2. **Token 传递**：子站首次通过 `_nav_token` URL 参数接收 Token
3. **中间件写入**：`subsite-middleware/auth_check.php` 自动将 Token 写入 Cookie 并清洗 URL

### 子站接入方式

**PHP 子站**：

```php
<?php
require_once '/var/www/nav/subsite-middleware/auth_check.php';
echo '当前用户：' . $nav_user['username'];
```

**Nginx auth_request**：

```nginx
location /protected/ {
    auth_request /auth/verify.php;
    proxy_pass http://backend/;
}
```

> 注意：`_nav_token` 的 Cookie 写入和 URL 清洗逻辑存在已知的代码结构问题（位于未登录分支的 exit 之后，常规执行流中不可达）。这是当前代码中的已知缺陷。

---

## 通知与 Webhook 架构

### 通知运行时

核心文件：`shared/.php`

**支持渠道**：Telegram、飞书、钉钉、企业微信、自定义 Webhook。

### 事件订阅

在 `admin/` 中配置渠道时，可订阅以下事件：

- `SUCCESS` — 登录成功
- `FAIL` — 登录失败
- `IP_LOCKED` — IP 被锁定
- `LOGOUT` — 用户登出
- `SETUP` — 完成安装向导
- `task_failed` — 计划任务失败

### 发送流程

```text
业务代码调用 notify_event('task_failed', [...])
  → notify_load_data() 读取 channels
  → 遍历 enabled 且订阅了该事件的渠道
  → notify_channel_is_due() 冷却检查（防止轰炸）
  → notify_send_channel()
    → notify_build_request() 按渠道类型构造 payload
    → notify_http_post_json() 发送
    → notify_channel_mark_sent() 更新运行时状态
    → notify_log_write() 写日志
```

### 日志位置

`data/logs/.log`

---

## DDNS 执行流程

### 触发方式

1. **手动执行**：后台 DDNS 页面点击"立即执行"
2. **定时执行**：Cron 定时调用 `cli/ddns_sync.php`

### 执行流程

```text
cron 触发 cli/ddns_sync.php
  → ddns_due_tasks() 读取到期任务
  → ddns_run_task($task)
    → 获取当前公网 IP（多来源 + 线路优选 + fallback）
    → dns_api_upsert() 调用 DNS 提供商 API
    → 更新 ddns_tasks.json runtime 状态
    → 写入 data/logs/ddns_{id}.log
  → 汇总退出码（0=全部成功，1=任一失败）
```

### IP 来源策略

支持多个公网 IP 检测源，按优先级和响应速度自动选择。如果主源失败，自动 fallback 到备用源。

---

## 计划任务调度原理

### 数据模型

每个任务包含：

```json
{
  "id": "task_id",
  "name": "任务名称",
  "script_filename": "task_id.sh",
  "schedule": "*/5 * * * *",
  "command": "echo hello",
  "enabled": true,
  "last_run": "2026-04-15 10:00:00",
  "last_code": 0,
  "last_output": "hello"
}
```

### 脚本落地

保存任务时，命令文本会被写入文件：

```text
data/tasks/task_id.sh
```

同时 Cron 调度表会同步更新（通过 `cron_regenerate()`）。

### 执行方式

1. **Cron 自动执行**：容器内 crond 按 schedule 触发
2. **后台立即执行**：点击"立即执行"，由 `cli/run_scheduled_task.php` 处理

### 日志文件

每个任务的日志保存在同名文件：

```text
data/tasks/task_id.log
```

### 调度器保护

防止多个执行实例同时运行同一个任务。通过锁文件机制实现。

### 超时与健壮性机制

- **硬超时**：`cron_execute_task()` 对 `proc_open` 启动的 bash 进程实施 3600 秒硬超时。超时后先发送 **SIGTERM**，等待 10 秒未退出则发送 **SIGKILL**，最终退出码记为 `124`。这能确保任何用户脚本都不会导致 PHP 进程永久挂起。
- **并发写保护**：`cron_store_run_result()` 在「读-改-写」`scheduled_tasks.json` 的全过程中持有 `flock(LOCK_EX)` 独占锁，彻底消除多个任务同时结束时结果互相覆盖的竞态。
- **僵尸锁检测**：任务锁文件记录当前 PHP 进程 PID。下次加锁时若 PID 已不存在，则自动判定为僵尸锁并清理，避免 OOM/SIGKILL 场景下锁文件永久残留。

---

## 关键数据流

### 登录流程

```text
POST login.php
  → csrf_check()
  → ip_locks_load() 检查锁定
  → auth_check_password() 验证密码
  → auth_generate_token() 生成 Token
  → auth_set_cookie() 写入 Cookie
  → auth_write_log('SUCCESS') + webhook_send()
  → 跳转首页或 redirect
```

### 保存代理站点 → Nginx 配置生效流程

```text
admin/sites.php POST save
  → save_sites() 写入 sites.json
  → 页面提示保存成功
  → 首页检测 homepage_pending_proxy_sites() 显示未生效横幅
  → admin/settings.php POST nginx_apply_and_reload
    → nginx_generate_proxy_conf() 生成 nav-proxy.conf / nav-proxy-domains.conf
    → admin_run_command('nginx -t && nginx -s reload')
    → 成功则更新 config.json nginx_last_applied
```

### DDNS 执行流程

```text
cron 触发 cli/ddns_sync.php
  → ddns_due_tasks() 读取到期任务
  → ddns_run_task($task)
    → 获取当前公网 IP（多来源 + 线路优选 + fallback）
    → dns_api_upsert() 调用 DNS 提供商 API
    → 更新 ddns_tasks.json runtime 状态
    → 写入 data/logs/ddns_{id}.log
  → 汇总退出码（0=全部成功，1=任一失败）
```

### 通知发送流程

```text
业务代码调用 notify_event('task_failed', [...])
  → notify_load_data() 读取 channels
  → 遍历 enabled 且订阅了该事件的渠道
  → notify_channel_is_due() 冷却检查
  → notify_send_channel()
    → notify_build_request() 按渠道类型构造 payload
    → notify_http_post_json() 发送
    → notify_channel_mark_sent() 更新运行时状态
    → notify_log_write() 写日志
```

---

## 测试架构分层

### 当前测试分层

| 层级 | 工具 | 覆盖范围 | 状态 |
|------|------|----------|------|
| Full E2E | Playwright | 所有 Web 页面、后台 CRUD、公共 API、跨页面链路 | **159 个 spec 文件，213 条测试** |
| CLI 回归 | Playwright + `runDockerPhp` | 主要 CLI 脚本参数、退出码、副作用 | 已覆盖 |
| 移动端专项 | Playwright (Pixel 7) | 首页、后台侧边栏、modal、长表单 | 已编写 |
| 单元测试 | PHPUnit | `shared/`、`admin/shared/` 核心逻辑 | 已建立基础套件 |

### Playwright 配置要点

- **默认基线**：`npx playwright test --project=chromium`
- **移动端**：`npx playwright test --project=mobile-chrome`
- **workers=1, fullyParallel=false**：串行执行，避免状态冲突
- **数据隔离**：创建型数据使用唯一值（时间戳），修改全局配置后需在 `try/finally` 中回滚
- **定位策略**：优先使用 `getByRole` / `getByLabel`，禁止把 `waitForTimeout` 当作主要同步手段

### PHPUnit 配置

- 配置文件：`phpunit.xml`
- 测试套件：`Shared`、`Admin`、`Subsite`
- Bootstrap：`tests/phpunit/bootstrap.php`（创建临时 DATA_DIR，测试结束后自动清理）

---

## 接口契约

### 公共 API（public/api/dns.php）

| Method | 参数 | 成功响应 | 失败响应 |
|--------|------|----------|----------|
| POST | `action=query&domain=xxx&type=A` | `{code:0, msg:"ok", data:{fqdn, zone, record_name, matches, records}}` | `{code:-1, msg:"..."}` |
| POST | `action=update&domain=xxx&value=1.2.3.4&type=A&ttl=600` | `{code:0, msg:"ok"}` | `{code:-1, msg:"..."}` |
| POST | `action=batch_update&domains=[...]&value=1.2.3.4` | `{code:0/-1, msg:"...", results:[...]}` | `{code:-1, msg:"..."}` |

### 后台通用 AJAX 响应格式

```json
{
  "ok": true,
  "msg": "操作成功提示",
  "data": { ... }
}
```

或

```json
{
  "ok": false,
  "msg": "错误提示"
}
```

---

## 部署环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `NAV_PORT` | 58080 | 容器内监听端口 |
| `TZ` | Asia/Shanghai | 时区 |
| `ADMIN` / `PASSWORD` / `NAME` / `DOMAIN` | — | 无人值守安装参数 |
| `NAV_DEV_MODE` | — | 开发模式，启用虚拟用户 `qatest` |
| `AUTH_SECRET_KEY` | — | 显式指定认证密钥 |
| `NAV_REQUEST_TIMING` | 1 | 请求耗时日志开关 |

### 推荐部署方式

```bash
docker compose up -d
```

数据目录 `./data` 必须挂载到容器 `/var/www/nav/data`。

---

## 安全注意事项

1. **认证机制**：JWT-like Token + Cookie HttpOnly + SameSite=Lax + 会话级撤销
2. **IP 锁定**：登录失败超过限制后自动锁定 IP
3. **docker.sock 挂载**：仅在需要 Docker 管理功能时临时挂载，完成后建议移除
5. **SSRF 防护**：所有根据用户输入发起外部 HTTP 请求的代码必须经过目标地址安全校验
6. **文件路径安全**：禁止直接使用用户输入拼接路径，优先使用 `basename()` 或正则白名单

---

*文档结束*
